# The production site key is deployed and App Check has already been verified.
ENFORCE_APP_CHECK=true
# Optional: keeps only the public catalog endpoint warm. Enabling this has a small monthly cost.
KEEP_CATALOG_WARM=false
# Public catalog cache per function instance (milliseconds).
CATALOG_CACHE_MS=60000

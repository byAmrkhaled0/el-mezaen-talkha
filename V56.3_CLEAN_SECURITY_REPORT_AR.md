# تقرير Production Hardening

## تم إصلاحه

- App Check secure-by-default في Production، rate limits متعددة الطبقات، وصلاحيات فروع default-deny للحسابات غير الإدارية.
- Cairo business time مركزي، booking request idempotency، كشف الحجز المطابق، أقفال مواعيد transaction-safe، وإعادة جدولة ذرية مع Shift/Leave validation.
- POS يحفظ شيكًا جديدًا نظيفًا بعد كل عملية، يدعم عاملًا لكل بند، ويستخدم server-side idempotency وreceipt code عشوائيًا فريدًا دون `count + 1`.
- فصل الطباعة وإعادة الطباعة عن الأثر المالي، ومنع حذف الإيرادات/الشيكات النهائية، وإضافة Void/Refund مع عكس المخزون والمكافآت والكاش.
- Cash Drawer وShift open/close وCash In/Out وربط المصروف وإغلاق اليوم وحساب variance من الخادم.
- Daily Dashboard باستعلامات Aggregation، Booking Calendar بفترة مرئية، Worker Leave، No-show، Low Stock، Customer 360، QR rotation/revocation، Pagination وConnection indicator.
- الإرسال التشغيلي الحالي يدوي بلا تكلفة Cloud API: الطباعة والمشاركة تستخدمان ملف PNG نفسه للشيك، ويستطيع الكاشير تجهيز رسالة العرض من بطاقة العرض وفتح محادثات العملاء على دفعات واحدة تلو الأخرى. يظل WhatsApp Cloud API الآلي مغلقًا افتراضيًا، مع campaign queue/batches/idempotency وWebhook جاهزين للتفعيل لاحقًا.
- Cache version جديد، عدم تخزين الشاشات الحساسة، مسارات Vercel/Firebase متوافقة، `/favicon.ico` صالح، وSEO يحجب admin/login/account.

## حدود التحقق

- `PASS`: 47/47 اختبارات تطبيق، 17/17 اختبارات Core/Functions، Production Build، 13 مسار Smoke، 190 رابط/Asset، وFirebase readiness.
- قياس ملفات Build محليًا: 80 طلبًا، concurrency=1، p50=7ms، p95=21ms، error rate=0%. هذا ليس قياس Firestore/Functions أو Production.
- الاختبارات المحلية وBuild لا تثبت نجاح Console configuration أو Meta templates/Callback registration أو Firestore indexes قبل نشرها.
- اختبارات race/load الحقيقية يجب أن تنفذ على Emulator/Staging بعد نشر الإعدادات، وليس على Production.
- `BLOCKED`: Firebase Rules Emulator لم يعمل في هذه الجلسة لأن `firebase-tools` يحتاج تنزيلًا خارجيًا؛ ملفات Rules نفسها default-deny وفحص wiring نجح، لكن لا يُسجل Emulator كـPASS.
- Backup/TTL/Monitoring وRPO/RTO إجراءات خارجية موثقة في `PHASE1_EXTERNAL_ACTIONS_AR.md`.
